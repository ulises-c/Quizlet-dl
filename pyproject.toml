[project]
name = "quizlet-dl"
version = "0.1.0"
description = "Download Quizlet study sets as JSON and Anki-ready TSV"
requires-python = ">=3.8"
dependencies = [
    "playwright",
    "playwright-stealth",
]
